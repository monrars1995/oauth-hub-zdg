{
  "name": "goldneuron-meta-apphub",
  "version": "1.1.0",
  "description": "Meta AppHub by @goldneuron.io: OAuth onboarding, authenticated Meta webhooks and secure event forwarding.",
  "license": "AGPL-3.0-only",
  "author": "@goldneuron.io",
  "homepage": "https://github.com/monrars1995/oauth-hub-zdg",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/monrars1995/oauth-hub-zdg.git"
  },
  "bugs": {
    "url": "https://github.com/monrars1995/oauth-hub-zdg/issues"
  },
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build && node --test --test-concurrency=1 test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
